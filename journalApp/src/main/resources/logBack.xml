<configuration>

    <!-- Console Appender -->
    <appender name="consoleAppender" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender -->
    <!-- File Appender with Rolling Enabled -->
    <appender name="fileAppender" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- The active log file -->
        <file>C:/Users/sonuh/Desktop/journalApp/journalApp/logs/journalApp.log</file>
        <!-- Rolling policy -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!--
            Roll every minute.
            %d{yyyy-MM-dd_HH-mm} → creates a new file each minute.
            -->
            <fileNamePattern>
                C:/Users/sonuh/Desktop/journalApp/journalApp/logs/journalApp.%d{yyyy-MM-dd_HH-mm}.log
            </fileNamePattern>
            <maxHistory>2</maxHistory>
        </rollingPolicy>
        <append>true</append>
        <encoder><pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern></encoder></appender>


    <!-- Root Logger: all logs go to console + file -->
    <root level="INFO">
        <appender-ref ref="consoleAppender" />
        <appender-ref ref="fileAppender" />
    </root>

</configuration>
